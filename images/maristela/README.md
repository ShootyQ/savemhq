# Maristela images

The Maristela pages currently use the ship photo at the site root:

- `maristela-ship.png` — main ship photo used on `maristela.html` and `maristela-logs.html`

The log page is now a data tracker. Entries are typed into the form, saved in the browser with local storage, and exportable as CSV.
