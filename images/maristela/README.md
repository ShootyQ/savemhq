# Maristela images

Add the real photos here with these filenames so the pages load them automatically:

- `maristela-ship.jpg` — main ship photo used on `maristela.html` and `maristela-logs.html`
- `log-page-1.jpg` — first scanned logbook page
- `log-page-2.jpg` — second scanned logbook page
- `log-page-3.jpg` — optional extra scan
- `log-page-4.jpg` — optional extra scan

The log page displays the images only and does not transcribe any handwritten voyage details.
