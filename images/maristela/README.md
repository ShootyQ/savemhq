# Maristela images

Add the real ship photo here with this filename so the pages load it automatically:

- `maristela-ship.jpg` — main ship photo used on `maristela.html` and `maristela-logs.html`

The log page is now a data tracker. Entries are typed into the form, saved in the browser with local storage, and exportable as CSV.
